# トレーサビリティマトリクス（RTM）

> 様式: [test-design-from-issue/test-design-matrix-template.md](../../../.claude/skills/test-design-from-issue/test-design-matrix-template.md)
> 要件→設計→Issue→テストを 1 表に集約し、横串でカバレッジ漏れを検出する正典。Issue 起票・実装ループで更新する。
> モード: **finalize**（Issue #3 実装完了後、実テストコードと突合して確定）

## 凡例

| 略号 | 正式名称 | 補足 |
| --- | --- | --- |
| TC-XXX | 単体テストケース ID | 3 桁ゼロ埋め |
| IT-XXX | 結合テストケース ID | 3 桁ゼロ埋め（結合テスト工程で記入） |
| E2E-XXX | E2E テストケース ID | 3 桁ゼロ埋め（現環境では別工程・空欄可） |
| AC-XXX | 受け入れ条件 | 要件定義 `functional/*.md` で定義 |
| IMPL-XX | 実装内容項目 | AC が無い基盤 Issue（#3）で設計書「実装内容」を観点化した一時 ID |
| BR-XXX | 業務ルール | 要件定義 `業務ルール.md` で定義 |
| SCR-XXX | 画面 ID | 3 桁ゼロ埋め |

## マトリクス

| UC | AC / 実装内容項目 | BR | SCR | API operationId | Issue# | TC-XXX | IT-XXX | E2E-XXX |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| —（基盤） | IMPL-01 DomainException 例外階層と HTTP/ErrorCode 対応 | — | — | 全 operationId（横断） | #3 | TC-001〜TC-013 | — | — |
| —（基盤） | IMPL-02 @RestControllerAdvice 共通マッピング | — | — | 全 operationId（横断） | #3 | TC-014〜TC-016, TC-037 | — | — |
| —（基盤） | IMPL-03 MethodArgumentNotValidException 専用ハンドラ | — | — | 全 operationId（横断） | #3 | TC-017, TC-018 | — | — |
| —（基盤） | IMPL-04 未捕捉 Exception → INTERNAL_ERROR | — | — | 全 operationId（横断） | #3 | TC-019, TC-020 | — | — |
| —（基盤） | IMPL-05 Clock / SystemClock | — | — | — | #3 | TC-021, TC-022 | — | — |
| —（基盤） | IMPL-06 共通識別子（TenantId/UserId/JobId） | — | — | — | #3 | TC-023〜TC-025 | — | — |
| —（基盤） | IMPL-07 EnumMapper | — | — | 一覧・詳細系 operationId（横断） | #3 | TC-026〜TC-028 | — | — |
| —（基盤） | IMPL-08 ロギング方式（レベル・機微情報マスク・MDC） | — | — | 全 operationId（横断） | #3 | TC-029〜TC-032 | — | — |
| —（基盤） | IMPL-09 ページング応答共通化（PageMeta） | — | — | listJobs, listMyBids, listNotifications 等 | #3 | TC-033, TC-034 | — | — |
| —（基盤） | IMPL-10 NotificationPublisher（Port） | — | — | — | #3 | TC-035 | — | — |
| —（基盤） | IMPL-11 MailSender（Port、雛形） | — | — | EXT-001 関連（実装は auth Issue 側） | #3 | TC-036 | — | — |

- AC が無い基盤 Issue のため「AC / 実装内容項目」列は実装内容項目（IMPL-XX ＋ 項目名）を入れており空欄にしていない。
- IT-XXX 列は結合テスト工程（`/integration-test-from-design`）が該当フィーチャの構成 Issue が組み上がった後に記入する（本 Issue 単独では対象外）。
- E2E-XXX は現環境では別工程のため `—`。
- TC 番号は本ファイル・当該リポジトリで初出（既存マトリクス無し）のため TC-001 から採番。以降の Issue は本マトリクスの最大 TC 番号の続番から採番する。

## finalize モードの整合状況（実コード突合済み）

- 本表は Issue #3 分を反映した確定版。TC-001〜TC-037 は実装済みテストコード（`src/test/java/.../shared/**`, `auth/domain/service/MailSenderTest`, `notification/domain/service/NotificationPublisherTest`）の `@DisplayName` と突合済みで、件数・シナリオともに一致する（TC-037: レビュー指摘対応で追加した `GlobalExceptionHandler.handleStateConflictException` の 409/CONFLICT マッピングテスト）。
- IT-XXX 列は結合テスト工程（`/integration-test-from-design`）が該当フィーチャの構成 Issue が組み上がった後に記入するため、本 Issue 単独の時点では `—`（未）のままでよい。
- E2E-XXX は現環境では別工程のため `—`。
